
import React from 'react';

// Initialize a Context using React.createContext()
const UserContext = React.createContext();

// Export the context as both a named and a default export.
export default UserContext;
export { UserContext };